import React, { Component } from 'react';
import { observer, inject } from 'mobx-react';
import cssModules from 'react-css-modules';
import styles from './<%= stylePath%>';
import {} from 'antd';

@inject('<%= modelsName%>')
@observer
@cssModules(styles)
class <%= modelsName%>Page extends Component {
  render() {
    return (
      <div>
        <p>You clicked1 times</p>
      </div>
    );
  }
}

export default <%= modelsName%>Page;
